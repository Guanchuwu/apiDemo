type Query {
  getMatchedReservation(items: [Item]): [Reservation]
}

input Item {
  partnerId: String
  confirmationNumber: String
}

type Reservation {
  reservationId: String
  reservationDetail: String
}
